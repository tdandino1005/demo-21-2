{
  "name": "mern-demos-21-2",
  "private": true,
  "dependencies": {
    "concurrently": "^7.6.0",
    "mongoose": "^7.0.1"
  },
  "scripts": {
    "client": "cd client && npm run dev",
    "install": "cd client && npm install && cd ../server && npm install",
    "server": "node --watch server/app/index.js",
    "start": "concurrently \"npm run server\" \"npm run client\""
  }
}
